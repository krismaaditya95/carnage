<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/colorSecondary1"
    tools:context="com.adit.carnage.Fragments.FragmentDua">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/tvFDua"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="TEXT_TITLE"
        android:textColor="@color/colorPrimary1"
        android:layout_margin="20dp"/>

    <Button
        android:id="@+id/btnOne"
        android:layout_below="@+id/tvFDua"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:text="OCR Camera 1 Activity"/>

    <Button
        android:id="@+id/btnTwo"
        android:layout_below="@+id/btnOne"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:text="OCR Camera 2 Activity"/>

</RelativeLayout>
